# CLAUDE.md

This is the **Autobooks iOS SDK distribution repository**. Pre-built xcframeworks only,
no source code.

This repo is a git submodule of the source repo. For full documentation, see the
source repo's `CLAUDE.md` (section "Package Distribution") and `RELEASE.md`.

## Quick Reference

- `./zip.sh` -- zip frameworks and compute SPM checksums
- Releases managed by `release.sh` in the source repo
